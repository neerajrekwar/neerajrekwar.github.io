// `app/page.tsx` is the UI for the `/` URL
export default function Page() {
  return(
    <footer
      className="h-[44vh] flex flex-col justify-center items-center bg-white dark:bg-black text-black dark:text-white"
      >hello, footer!</footer>
  );
}
